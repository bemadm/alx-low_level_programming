#include "main.h"
#include <string.h>
/**
* _strcmp -> this a function for comparing purpose
* @s1 : first parameter
* @s2 : second parameter
* Return: integer
*/
int _strcmp(char *s1, char *s2)
{
int result;

result = strncpy(s1, s2);
return (result);
